import { NavbarPathConverterPipe } from './navbar-path-converter.pipe';

describe('NavbarPathConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new NavbarPathConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
